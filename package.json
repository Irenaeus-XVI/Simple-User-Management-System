{
  "name": "simple-user-management-system",
  "version": "1.0.0",
  "description": "server-side application in Node.js to manage user accounts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ehab",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.2"
  }
}
